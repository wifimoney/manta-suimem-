version: "3.8"

services:
  # ============================================================
  # Postgres + pgvector
  # ============================================================
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: manta_recall
      POSTGRES_USER: manta
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-manta_dev_password}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/migrations:/docker-entrypoint-initdb.d
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U manta -d manta_recall"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ============================================================
  # Manta Indexer (TypeScript)
  # ============================================================
  indexer:
    build:
      context: ./indexer
      dockerfile: Dockerfile
    environment:
      SUI_RPC_URL: ${SUI_RPC_URL:-https://fullnode.testnet.sui.io:443}
      MANTA_PACKAGE_ID: ${MANTA_PACKAGE_ID}
      DATABASE_URL: postgres://manta:${POSTGRES_PASSWORD:-manta_dev_password}@postgres:5432/manta_recall
      NETWORK: ${NETWORK:-testnet}
      POLL_INTERVAL_MS: ${POLL_INTERVAL_MS:-2000}
      BATCH_SIZE: ${BATCH_SIZE:-50}
      MODULE_NAME: ${MODULE_NAME:-memory}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  # ============================================================
  # Phase 2+ services (uncomment when ready)
  # ============================================================

  # embedder:
  #   build: ./embedder
  #   environment:
  #     MODEL_NAME: nomic-ai/nomic-embed-text-v1.5
  #     DEVICE: cpu
  #   ports:
  #     - "8081:8081"

  # api:
  #   build: ./api
  #   environment:
  #     DATABASE_URL: postgres://manta:${POSTGRES_PASSWORD:-manta_dev_password}@postgres:5432/manta_recall
  #     EMBEDDER_URL: http://embedder:8081
  #     SUI_RPC_URL: ${SUI_RPC_URL:-https://fullnode.testnet.sui.io:443}
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - postgres
  #     - embedder

volumes:
  pgdata:
